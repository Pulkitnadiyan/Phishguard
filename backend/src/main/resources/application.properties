spring.application.name=phishguard-backend
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/phishguard_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=nadiyan06
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ML Service URL
ml.service.url=http://localhost:5000/predict

# Logging
logging.file.name=backend.log
